# 功能验证原子性测试方案

> **测试日期**: 2026-04-30
> **版本**: v1.2.2

## 测试策略

- 每个测试用例描述为一个完整的用户场景
- 包含：前置条件 → 操作步骤 → 预期结果 → 验证方式
- 测试设计遵循原子性原则：一个测试用例验证一个明确的功能点

---

## P0 级别测试用例

### T-001: 配方删除防呆验证

| 属性 | 内容 |
|------|------|
| **场景** | 用户尝试删除已被其他配方引用的配方 |
| **前置条件** | 创建配方 A，然后配方 B 添加配方 A 作为子配方 |
| **操作步骤** | 在配方列表选中配方 A，点击删除 |
| **预期结果** | 弹出错误提示："此配方已被 X 个其他配方引用，请先移除相关引用后删除" |
| **验证方式** | 配方 A 未被删除，仍存在于列表中 |

### T-002: 删除无引用配方

| 属性 | 内容 |
|------|------|
| **场景** | 用户删除一个没有被引用的配方 |
| **前置条件** | 创建一个新配方 C，无任何配方引用它 |
| **操作步骤** | 选中配方 C，点击删除，确认删除 |
| **预期结果** | 配方 C 成功删除，列表中不再显示 |
| **验证方式** | 配方 C 无法再次被选中/查看 |

---

### T-003: 配方成本计算 - 纯材料配方

| 属性 | 内容 |
|------|------|
| **场景** | 计算只含原材料的配方成本 |
| **前置条件** | 配方 only_materials: 含 3 种材料，每种有批次库存价格 |
| **操作步骤** | 打开配方详情，查看成本计算结果 |
| **预期结果** | 总成本 = Σ(数量 × 单价 × (1+损耗率))，单成本 = 总成本/产出量 |
| **验证方式** | 手动计算材料成本，与显示值对比 |

### T-004: 配方成本计算 - 含子配方

| 属性 | 内容 |
|------|------|
| **场景** | 计算包含子配方的配方成本 |
| **前置条件** | 子配方 sub_cost 输出量 10，成本 50；主配方含子配方 2 份 |
| **操作步骤** | 查看主配方成本 |
| **预期结果** | 子配方成本正确递归计入（2 × 50 = 100） |
| **验证方式** | 查看成本明细，子配方项显示递归后的单成本 |

### T-005: 深度递归保护

| 属性 | 内容 |
|------|------|
| **场景** | 配方 A → B → C → ... 超过 10 层递归 |
| **前置条件** | 创建深度 > 10 的配方引用链 |
| **操作步骤** | 尝试计算最顶层配方成本 |
| **预期结果** | 返回错误或限制深度后返回部分结果 |
| **验证方式** | 不应导致应用崩溃/卡死 |

### T-006: 循环引用保护

| 属性 | 内容 |
|------|------|
| **场景** | 配方 A 添加配方 B，配方 B 添加配方 A |
| **操作步骤** | 计算配方 A 成本 |
| **预期结果** | 不会无限递归，应用正常响应 |
| **验证方式** | 3 秒内返回结果 |

---

### T-007: 遥测 URL 白名单 - 有效 URL

| 属性 | 内容 |
|------|------|
| **场景** | 用户配置白名单内的 webhook URL |
| **前置条件** | 环境变量 `CUCKOO_TELEMETRY_ALLOWLIST` 含 `https://example.com/webhook` |
| **操作步骤** | 调用 `report_telemetry` 并传入该 URL |
| **预期结果** | 成功发送遥测数据 |
| **验证方式** | 服务器收到请求 |

### T-008: 遥测 URL 白名单 - 无效 URL

| 属性 | 内容 |
|------|------|
| **场景** | 用户传入不在白名单的 URL |
| **前置条件** | 白名单仅允许 `https://example.com/webhook` |
| **操作步骤** | 调用 `report_telemetry` 并传入 `https://evil.com/telemetry` |
| **预期结果** | 返回错误："webhook URL 不在白名单中" |
| **验证方式** | 请求被拒绝，未发出 |

### T-009: 遥测 - 无 URL 使用默认

| 属性 | 内容 |
|------|------|
| **场景** | 用户未指定 URL，使用默认端点 |
| **前置条件** | 配置了默认遥测 URL |
| **操作步骤** | 调用 `report_telemetry` 不传 webhook_url |
| **预期结果** | 使用默认 URL 发送 |
| **验证方式** | 默认服务器收到请求 |

---

### T-010: 调试打印文件名安全 - 含路径

| 属性 | 内容 |
|------|------|
| **场景** | 用户尝试使用路径分隔符作为文件名 |
| **前置条件** | 进入调试打印模式 |
| **操作步骤** | 输入文件名 `../../../etc/passwd` |
| **预期结果** | 文件被拒绝，使用默认文件名 |
| **验证方式** | 生成文件名不含路径成分 |

### T-011: 调试打印文件名安全 - 含 ..

| 属性 | 内容 |
|------|------|
| **场景** | 用户尝试使用 .. 跳转目录 |
| **前置条件** | 进入调试打印模式 |
| **操作步骤** | 输入文件名 `..` 或 `test..` |
| **预期结果** | 文件被拒绝，使用默认文件名 |
| **验证方式** | 文件写入受控目录，无目录穿越 |

### T-012: 调试打印文件名安全 - 特殊字符

| 属性 | 内容 |
|------|------|
| **场景** | 用户输入特殊字符 |
| **前置条件** | 进入调试打印模式 |
| **操作步骤** | 输入文件名 `test@#$%` |
| **预期结果** | 特殊字符被过滤，只保留字母数字下划线连字符 |
| **验证方式** | 生成有效文件名 |

### T-013: 返回路径安全

| 属性 | 内容 |
|------|------|
| **场景** | 调试打印后检查返回路径 |
| **前置条件** | 执行调试打印 |
| **操作步骤** | 查看返回的 file_path |
| **预期结果** | 只返回文件名，不含绝对路径（安全考虑） |
| **验证方式** | 路径字符串不包含父目录路径 |

---

## P1 级别测试用例

### T-014: 循环引用检测 - 同配方添加自身

| 属性 | 内容 |
|------|------|
| **场景** | 用户尝试将配方添加为自身的子配方 |
| **前置条件** | 打开配方编辑界面 |
| **操作步骤** | 在配方中添加自身作为子配方 |
| **预期结果** | 弹出错误："不能添加自身作为子配方" |
| **验证方式** | 添加操作被阻止 |

### T-015: 循环引用警告

| 属性 | 内容 |
|------|------|
| **场景** | 用户添加子配方后形成相互引用 |
| **前置条件** | 配方 X 被配方 Y 引用；打开配方 X 编辑 |
| **操作步骤** | 添加配方 Y 作为子配方 |
| **预期结果** | 弹出警告提示，确认后才添加 |
| **验证方式** | 用户可取消添加操作 |

### T-016: 菜品可售状态显式设定

| 属性 | 内容 |
|------|------|
| **场景** | 用户显式设置菜品为可售/不可售 |
| **前置条件** | 菜品当前为"不可售" |
| **操作步骤** | 点击切换为"可售" |
| **预期结果** | 菜品状态直接设为 true，而不是 toggle |
| **验证方式** | 数据库 is_available = 1 |

### T-017: 批量设置菜品可售

| 属性 | 内容 |
|------|------|
| **场景** | 批量设置多个菜品为可售 |
| **前置条件** | 选中多个菜品 |
| **操作步骤** | 点击"批量设为可售" |
| **预期结果** | 所有选中菜品状态统一设为 true |
| **验证方式** | 数据库记录确认 |

---

### T-018: 打印预览 HTML 安全渲染

| 属性 | 内容 |
|------|------|
| **场景** | 材料/供应商名称含 HTML 特殊字符 |
| **前置条件** | 材料名 `<script>alert(1)</script>` |
| **操作步骤** | 打印预览 |
| **预期结果** | 特殊字符被转义，不执行 JS |
| **验证方式** | 页面源码中显示为转义后文本 |

### T-019: 打印模板预览安全

| 属性 | 内容 |
|------|------|
| **场景** | 打印模板中使用动态字段 |
| **前置条件** | 在模板中插入材料名等动态字段 |
| **操作步骤** | 预览���板 |
| **预期结果** | 内容经过 DOMPurify 消毒 |
| **验证方式** | 无 XSS 风险 |

---

## 测试数据准备清单

| 编号 | 名称 | 类型 | 用途 |
|------|------|------|------|
| TD-001 | 材料-面粉 | Material | 成本计算 |
| TD-002 | 材料-糖 | Material | 成本计算 |
| TD-003 | 配方-面团 | Recipe | 子配方测试 |
| TD-004 | 配方-蛋糕 | Recipe | 主配方测试 |
| TD-005 | 批次-面粉-001 | InventoryBatch | 价格数据 |
| TD-006 | 菜品-蛋糕卷 | MenuItem | 可售状态测试 |

---

## 测试执行顺序建议

1. **安全类优先**: T-001, T-006, T-008, T-010, T-018
2. **核心功能次之**: T-003, T-004, T-016
3. **边界条件最后**: T-005, T-006, T-011

---

*本方案由 AI 于 2026-04-30 生成*